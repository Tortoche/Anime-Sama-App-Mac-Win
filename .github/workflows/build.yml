name: Build Anime-Sama Ultra 🌮

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: 📥 Récupération du Code
        uses: actions/checkout@v4

      - name: 💿 Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Nettoyage spécial pour Windows (évite les bugs de permission)
      - name: 🧹 Nettoyage Préalable (Windows)
        if: runner.os == 'Windows'
        run: |
          if (Test-Path node_modules) { Remove-Item -Recurse -Force node_modules }
          if (Test-Path package-lock.json) { Remove-Item -Force package-lock.json }
        shell: pwsh

      - name: 🌯 Installation des Ingrédients
        run: npm install --legacy-peer-deps

      - name: 🍳 Cuisine (Build - Sans Publication)
        # On ajoute "-p never" pour dire : Ne publie pas, donne juste le fichier !
        run: |
          if [ "$RUNNER_OS" == "Windows" ]; then
            npm run dist -- --win -p never
          else
            npm run dist -- --mac -p never
          fi
        shell: bash

      - name: 📦 Envoi du Colis (Upload)
        uses: actions/upload-artifact@v4
        with:
          name: App-${{ matrix.os }}
          path: |
            dist/*.exe
            dist/*.dmg
