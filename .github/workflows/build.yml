name: Build Anime-Sama Ultra 🌮

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: 📥 Récupération du Code
        uses: actions/checkout@v4

      - name: 💿 Installation de Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🌯 Installation des Ingrédients
        run: npm install --legacy-peer-deps

      - name: 🍳 Cuisine (Build et Publication)
        # On définit le token ici pour qu'il soit accessible par electron-builder
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # On lance le build. electron-builder détectera le GITHUB_TOKEN automatiquement.
          npm run dist
        shell: bash

      - name: 📦 Envoi du Colis (Upload Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: App-${{ matrix.os }}
          path: |
            dist/*.exe
            dist/*.dmg
